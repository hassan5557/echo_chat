// Root settings for Flutter Android build. Keep minimal.
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
// Android subproject is configured in android/settings.gradle.kts
